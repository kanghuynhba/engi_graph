# EngiGraph

EngiGraph is a RAG-powered engineering blog knowledge base.

Run locally:

```bash
poetry install
poetry run uvicorn app.main:app --reload
```

The API starts at `http://127.0.0.1:8000`.
